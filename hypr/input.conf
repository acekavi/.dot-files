# Input Configuration
# Keyboard, mouse, and touchpad settings

# Keyboard configuration
input {
    kb_layout = us
    kb_variant = 
    kb_model = 
    kb_options = 
    kb_rules = 
    
    # macOS-style keyboard behavior
    repeat_rate = 50
    repeat_delay = 300
    numlock_by_default = true
    resolve_binds_by_sym = false
    
    # Mouse configuration
    follow_mouse = 1
    mouse_refocus = true
    float_switch_override_focus = 2
    
    # Mouse sensitivity (macOS-style)
    sensitivity = 0.0
    accel_profile = flat
    force_no_accel = false
    left_handed = false
    scroll_method = 2fg
    scroll_button = 0
    scroll_button_lock = 0
    scroll_points = 
    natural_scroll = false
    
    # Touchpad configuration (macOS-style)
    touchpad {
        natural_scroll = true
        disable_while_typing = true
        tap-to-click = true
        tap-and-drag = true
        drag_lock = false
        middle_button_emulation = false
        clickfinger_behavior = true
        scroll_factor = 1.0
    }
    
    # Tablet configuration
    tablet {
        transform = 0
        output = 
        region_position = 0 0
        region_size = 0 0
        relative_input = false
    }
}

# Gesture configuration (macOS-style trackpad gestures)
gestures {
    workspace_swipe = true
    workspace_swipe_fingers = 3
    workspace_swipe_distance = 300
    workspace_swipe_invert = true
    workspace_swipe_min_speed_to_force = 30
    workspace_swipe_cancel_ratio = 0.5
    workspace_swipe_create_new = true
    workspace_swipe_direction_lock = true
    workspace_swipe_direction_lock_threshold = 10
    workspace_swipe_forever = false
    workspace_swipe_use_r = false
}

# Per-device input configuration examples
# Uncomment and modify as needed for your specific devices

# Example: External mouse configuration
# device {
#     name = logitech-mx-master-3
#     sensitivity = -0.2
#     accel_profile = adaptive
#     natural_scroll = false
# }

# Example: External keyboard configuration
# device {
#     name = keychron-k2
#     kb_layout = us
#     kb_options = caps:escape
#     repeat_rate = 60
#     repeat_delay = 250
# }

# Example: Graphics tablet configuration
# device {
#     name = wacom-intuos-pro
#     transform = 0
#     output = DP-1
#     region_position = 0 0
#     region_size = 1920 1080
# }

# Example: Trackball configuration
# device {
#     name = kensington-expert-mouse
#     sensitivity = 0.3
#     accel_profile = flat
#     scroll_method = button
#     scroll_button = 274
#     left_handed = false
# }
